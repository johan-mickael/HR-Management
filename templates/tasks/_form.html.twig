{% form_theme form 'bootstrap_5_layout.html.twig' %}
<div class="mt-2 shadow-sm border p-3 bg-white w-50 card">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success btn-md">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>